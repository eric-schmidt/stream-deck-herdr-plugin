import streamDeck from "@elgato/streamdeck";

// Placeholder entry point created during scaffolding.
// The real wiring (store + actions) is implemented in the final task.
streamDeck.logger.setLevel("info");
streamDeck.connect();
